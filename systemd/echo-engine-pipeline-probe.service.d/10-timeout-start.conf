# Drop-in overlay for echo-engine-pipeline-probe.service
# ECHO R&D proposal #10 (rnd-10): TimeoutStartSec=300
#
# Apply (ops, outside this PR):
#   sudo cp -a systemd/echo-engine-pipeline-probe.service.d /etc/systemd/system/
#   sudo systemctl daemon-reload
# Do NOT apply from CertForge / builder sandboxes.

[Service]
TimeoutStartSec=300
